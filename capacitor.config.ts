import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.eval01',
  appName: 'eval01',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
